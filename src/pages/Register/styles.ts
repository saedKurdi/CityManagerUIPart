// importing styled components for creating :
import styled from "styled-components";

// creating form for registering user : 
export const RegisterForm = styled.form`
    display: flex;
    flex-direction: column;
    gap: 8px;
`;

// creating input for register form : 
export const RegisterFormInput = styled.input`
    padding: 10px 20px;
    border: none;
`;

// creating button for register form : 
export const RegisterFormButton = styled.button`
    padding: 5px;
    border-radius: 15px;
    cursor: pointer;
`;